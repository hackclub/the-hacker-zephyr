# Generated by watcher (see /opt/zephyrnet/watcher)

server {
        listen 80;
        listen 443 ssl;

        server_name {{site}} www.{{site}};
	
	ssl_certificate     /etc/ssl/private/zephyr.crt;
	ssl_certificate_key /etc/ssl/private/zephyr.key;

        root /opt/zephyrnet/{{site}};

        index index.html index.htm index.nginx-debian.html;

        location / {
                try_files $uri $uri/ =404;
        }
}
